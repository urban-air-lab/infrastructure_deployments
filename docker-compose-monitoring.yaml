services:
  node_exporter:
    image: quay.io/prometheus/node-exporter:v1.10.2
    container_name: node_exporter
    restart: always
    ports:
      - "9100:9100"
    networks:
      - monitoring
    command:
      - '--path.procfs=/host/proc'
      - '--path.sysfs=/host/sys'
      - '--collector.filesystem.ignored-mount-points=^/(sys|proc|dev|host|etc)($|/)'
    pid: host
    volumes:
      - /proc:/host/proc:ro
      - /sys:/host/sys:ro
      - /:/rootfs:ro
    healthcheck:
      test: [ "CMD", "wget", "--spider", "http://localhost:9100/" ]
      interval: 1m
      timeout: 10s
      retries: 5

  cadvisor:
    image: gcr.io/cadvisor/cadvisor:v0.52.1
    container_name: cadvisor
    restart: always
    ports:
      - "8080:8080"
    networks:
      - monitoring
    volumes:
      - /:/rootfs:ro
      - /run:/run:ro
      - /sys:/sys:ro
      - /var/lib/docker/:/var/lib/docker:ro
      - /dev/disk/:/dev/disk:ro
    devices:
      - /dev/kmsg
    privileged: true
    healthcheck:
      test: [ "CMD", "wget", "--spider", "http://localhost:8080/containers/" ]
      interval: 1m
      timeout: 10s
      retries: 5

  prometheus:
    image: docker.io/prom/prometheus:v3.6.0
    container_name: prometheus
    restart: always
    networks:
      - monitoring
    command:
      - --config.file=/etc/prometheus/prometheus.yaml
      - --storage.tsdb.retention.time=7d
      - --storage.tsdb.retention.size=2GB
    ports:
      - "9090:9090"
    volumes:
      - ${CONFIG_ROOT}/configs/prometheus/prometheus.yaml:/etc/prometheus/prometheus.yaml:ro
      - ${CONFIG_ROOT}/configs/prometheus/alerts.yaml:/etc/prometheus/alerts.yaml:ro
      - prometheus-data:/prometheus
    healthcheck:
      test: [ "CMD", "wget", "--spider", "http://localhost:9090/-/healthy" ]
      interval: 1m
      timeout: 10s
      retries: 5

  alertmanager:
    image: alertmanager-envsubst:v0.31.1
    build: ./docker/alertmanager-docker
    container_name: alertmanager
    restart: always
    ports:
      - "9093:9093"
    entrypoint:
      - /bin/sh
      - -c
      - |
        envsubst < /etc/alertmanager/alertmanager.yaml > /tmp/alertmanager.yaml &&
        exec /bin/alertmanager --config.file=/tmp/alertmanager.yaml
    volumes:
      - ${CONFIG_ROOT}/configs/alertmanager/alertmanager.yaml:/etc/alertmanager/alertmanager.yaml:ro
    environment:
      - SMTP_HOST=${SMTP_HOST}
      - SMTP_USER=${SMTP_USER}
      - SMTP_AUTH=${SMTP_AUTH}
    networks:
      - monitoring
    healthcheck:
      test: [ "CMD", "wget", "--spider", "http://localhost:9093/-/healthy" ]
      interval: 1m
      timeout: 10s
      retries: 5

  grafana:
    image: grafana/grafana:13.1.0-25410206825-ubuntu
    container_name: grafana
    restart: always
    networks:
      - iot
      - frontend
      - monitoring
    ports:
      - "3000:3000"
    volumes:
      - ${CONFIG_ROOT}/configs/grafana-provisioning:/etc/grafana/provisioning:ro
      - grafana-data:/var/lib/grafana
    environment:
      - GF_SECURITY_ADMIN_USER=${GF_SECURITY_ADMIN_USER}
      - GF_SECURITY_ADMIN_PASSWORD=${GF_SECURITY_ADMIN_PASSWORD}
      - DOCKER_INFLUXDB_INIT_ADMIN_TOKEN=${DOCKER_INFLUXDB_INIT_ADMIN_TOKEN}
      - GF_SMTP_ENABLED=true
      - GF_SMTP_HOST=${SMTP_HOST}
      - GF_SMTP_USER=${SMTP_USER}
      - GF_SMTP_PASSWORD=${SMTP_AUTH}
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=ual_infrastructure_deployments_frontend"

      - "traefik.http.routers.grafana.rule=Host(`grafana.${TRAEFIK_DOMAIN}`)"
      - "traefik.http.routers.grafana.entrypoints=websecure"
      - "traefik.http.routers.grafana.tls=true"
      - "traefik.http.routers.grafana.tls.certresolver=myresolver"
    healthcheck:
      test: [ "CMD-SHELL", "curl -f localhost:3000/api/health && echo 'ready'" ]
      interval: 1m
      timeout: 10s
      retries: 5

networks:
  iot:
  frontend:
  monitoring:

volumes:
  grafana-data:
  prometheus-data:
