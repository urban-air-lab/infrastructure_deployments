name: "CI/CD Pipeline"

on:
  push:
    branches: [ "feature/ci-cd" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - name: Trust remote host key
        run: |
          mkdir -p ~/.ssh
          ssh-keyscan -H docker.example.com >> ~/.ssh/known_hosts

      - name: Docker via SSH
        env:
          DOCKER_HOST: ssh://root@docker.example.com
        run: |
          docker version
          docker ps
          
